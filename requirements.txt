fastapi==0.139.0          # Web framework (latest patches, optimized async handling)
uvicorn[standard]==0.44.0 # ASGI server (improved websocket and keep-alive stability)
aiofiles==24.1.0          # Async file I/O (updated file stream wrappers)
janus==1.0.0              # Async/thread queue bridge (still fully stable)
aiohttp==3.11.2           # Async HTTP (critical security patches)

# --- Core YouTube Downloader Suite ---
yt-dlp>=2026.07.09        # YouTube downloader core (latest patch against YouTube throttling)
yt-dlp-ejs>=2026.03.17    # Matching JavaScript solver package
